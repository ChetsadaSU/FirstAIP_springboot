spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/test012345
    username: postgres
    password: syspass

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
